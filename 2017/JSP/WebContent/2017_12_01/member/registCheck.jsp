<%@ page language="java" contentType="text/html; charset=UTF-8"
    pageEncoding="UTF-8"%>

<%
	int count = (int)application.getAttribute("memCount");
	/*int memCount = 1;
	if(count == 1){
		session.setAttribute("memCount", memCount);
		application.setAttribute("memCount", memCount++);		
	}else{
		session.setAttribute("memCount", count);
		application.setAttribute("memCount", count++);		
	}*/	
%>
	